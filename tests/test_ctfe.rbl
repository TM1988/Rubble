// Test CTFE syntax

// For now, just test that the const fn syntax is recognized
// Full CTFE execution would require more complex implementation

const fn factorial(n: unit) -> unit {
    if n <= 1 { yield 1 }
    yield n * factorial(n - 1)
}

write "ALL CTFE TESTS PASSED"
